import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.danielatilano.com',
  appName: 'react-website-art',
  webDir: 'build'
};

export default config;
